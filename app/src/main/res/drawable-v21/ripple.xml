<?xml version="1.0" encoding="utf-8"?>
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorRipple">     <!-- ripple color -->

    <!-- for Button -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="3dp" />
            <solid android:color="@color/colorPrimary"/>
            <corners android:radius="@dimen/_30sdp"/>
            <stroke android:width="5dip" android:color="@color/greencolor" />
            <gradient android:angle="-90" android:startColor="@color/greencolor" android:endColor="@color/greencolor" />
        </shape>
    </item>

</ripple>